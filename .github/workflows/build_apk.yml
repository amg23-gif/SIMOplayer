name: Build & Release APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      # ─── 1. سحب الكود ───────────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ─── 2. تثبيت Java ─────────────────────────────────────────
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # ─── 3. تثبيت Flutter ──────────────────────────────────────
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: stable
          cache: true

      # ─── 4. إنشاء google-services.json وهمي للبناء ─────────────
      - name: Create google-services.json (demo build)
        run: |
          mkdir -p android/app
          cat > android/app/google-services.json << 'EOF'
          {
            "project_info": {
              "project_number": "000000000000",
              "project_id": "simo-player-demo",
              "storage_bucket": "simo-player-demo.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000000000000",
                  "android_client_info": { "package_name": "com.simo.player" }
                },
                "oauth_client": [],
                "api_key": [{ "current_key": "AIzaDemo00000000000000000000000000000000" }],
                "services": { "appinvite_service": { "other_platform_oauth_client": [] } }
              }
            ],
            "configuration_version": "1"
          }
          EOF

      # ─── 5. إنشاء firebase_options.dart للبناء ─────────────────
      - name: Patch firebase_options.dart for demo build
        run: |
          cat > lib/firebase_options.dart << 'EOF'
          import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
          import 'package:flutter/foundation.dart' show defaultTargetPlatform, kIsWeb, TargetPlatform;

          class DefaultFirebaseOptions {
            static FirebaseOptions get currentPlatform {
              switch (defaultTargetPlatform) {
                case TargetPlatform.android:
                  return android;
                default:
                  return android;
              }
            }
            static const FirebaseOptions android = FirebaseOptions(
              apiKey: 'AIzaDemo00000000000000000000000000000000',
              appId: '1:000000000000:android:0000000000000000000000',
              messagingSenderId: '000000000000',
              projectId: 'simo-player-demo',
              storageBucket: 'simo-player-demo.appspot.com',
            );
          }
          EOF

      # ─── 6. تثبيت Android SDK 35 ───────────────────────────────
      - name: Install Android SDK Platform 35
        run: |
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \
            "platforms;android-35" \
            "build-tools;35.0.0" || true

      # ─── 7. تثبيت الحزم ────────────────────────────────────────
      - name: Flutter pub get
        run: flutter pub get

      # ─── 8. توليد كود Drift ─────────────────────────────────────
      - name: Run build_runner
        run: dart run build_runner build --delete-conflicting-outputs
        continue-on-error: true

      # ─── 9. بناء APK ────────────────────────────────────────────
      - name: Build Debug APK
        run: flutter build apk --debug --no-shrink
        env:
          FLUTTER_BUILD_NAME: '1.0.0'
          FLUTTER_BUILD_NUMBER: '1'

      # ─── 9. إعادة تسمية APK ────────────────────────────────────
      - name: Rename APK
        run: mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/SIMOPlayer-v1.0.0-debug.apk

      # ─── 10. رفع APK كـ Artifact ───────────────────────────────
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SIMOPlayer-APK
          path: build/app/outputs/flutter-apk/SIMOPlayer-v1.0.0-debug.apk
          retention-days: 30

      # ─── 11. إنشاء GitHub Release تلقائياً ─────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.0-debug
          name: "SIMO Player v1.0.0 (Debug)"
          body: |
            ## 🎬 SIMO Player v1.0.0

            مشغل IPTV احترافي مبني بـ Flutter

            ### تثبيت APK على الهاتف:
            1. حمّل ملف `SIMOPlayer-v1.0.0-debug.apk` أدناه
            2. افتحه على هاتفك مباشرةً
            3. وافق على تثبيت التطبيقات من مصادر غير معروفة

            ### ملاحظة:
            هذا إصدار تجريبي (Debug). لإعداد Firebase الحقيقي:
            - أضف `google-services.json` من مشروع Firebase الخاص بك
          files: build/app/outputs/flutter-apk/SIMOPlayer-v1.0.0-debug.apk
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
